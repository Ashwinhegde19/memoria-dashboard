// This service has been disabled as per user request to remove AI features.
// It is kept as a placeholder to prevent import errors during refactoring if referenced elsewhere,
// though it should not be used.

export const generateSystemState = async () => {
  throw new Error("AI Generation is disabled.");
};

export const analyzeSystemLogs = async () => {
  throw new Error("AI Analysis is disabled.");
};